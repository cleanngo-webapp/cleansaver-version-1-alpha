# Node modules (we rebuild them inside vite-builder anyway)
node_modules
npm-debug.log
yarn-error.log
pnpm-lock.yaml

# PHP dependencies (handled by composer inside container)
vendor

# Environment & configs
.env
.env.*
!.env.example

# OS / editor junk
.DS_Store
Thumbs.db
.idea
.vscode
*.swp

# Build artifacts (we only copy what vite-builder makes)
public/build

# Git
.git
.gitignore
